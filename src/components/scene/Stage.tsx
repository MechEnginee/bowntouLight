// components/scene/Stage.tsx
// 무대 기본 환경 — 바닥/뒷벽은 반사 표면 오브젝트(floor-1/wall-1),
// 트러스 프레임은 트러스 바 오브젝트(bar-1/bar-2)로 관리된다.
// 현재 추가 하드코딩 요소 없음(향후 정적 소품용 자리).

export function Stage() {
  return null;
}
